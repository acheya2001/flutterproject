import 'package:flutter/material.dart';import '../../features/admin_compagnie/presentation/pages/admin_compagnie_dashboard.dart';import '../../features/expert/presentation/pages/expert_dashboard.dart';import '../../features/auth/presentation/pages/login_page.dart';import '../../features/auth/presentation/pages/change_password_page.dart';/// 🗺️ Configuration des routes de l';  static const String login = '/login';  static const String changePassword = '/change-password';  static const String superAdminDashboard = '/super-admin-dashboard';  static const String compagnieDashboard = '/compagnie-dashboard';  static const String agenceDashboard = '/agence-dashboard';  static const String agentDashboard = '/agent-dashboard';  static const String expertDashboard = '/expert-dashboard';  static const String conducteurDashboard = '/conducteur-dashboard';  static Widget _buildSuperAdminDashboard(';        title: const Text(';            const SizedBox(height: 32')')';              ';  static Route<dynamic>? onGenerateRoute(RouteSettings settings';    final uri = Uri.parse(settings.name ?? ';    if (uri.pathSegments.isNotEmpty && uri.pathSegments[0] == ';        ';    if (uri.pathSegments.isNotEmpty && uri.pathSegments[0] == ';        ';    if (uri.pathSegments.isNotEmpty && uri.pathSegments[0] == ';  static Widget _buildAgenceDashboard(String agenceId';        title: const Text(';            const SizedBox(height: 8')')';            (agenceId';              ';  static Widget _buildAgentDashboard(String agenceId';        title: const Text(';            const SizedBox(height: 8')')';            (agenceId';              ';  static Widget _buildConducteurDashboard(';        title: const Text(';            const SizedBox(height: 32')')';              ';  static bool requiresAuth(String route';    if (route.startsWith('/super-admin)';      return 'super_admin';    } else if (route.startsWith('/compagnie-dashboard)';      return 'admin_compagnie';    } else if (route.startsWith('/agence-dashboard)';      return 'admin_agence';    } else if (route.startsWith('/agent-dashboard)';      return 'agent_agence';    } else if (route.startsWith('/expert-dashboard)';      return 'expert_auto';    } else if (route.startsWith('/conducteur-dashboard)';      return ';    switch (role';      case 'super_admin';      case 'admin_compagnie';        return compagnieId != null ? '/compagnie-dashboard/'compagnieId';      case 'admin_agence';        return agenceId != null ? '/agence-dashboard/'agenceId';      case 'agent_agence';        return agenceId != null ? '/agent-dashboard/'agenceId';      case 'expert_auto';      case ';  static String getPageTitle(String route';    if (route.startsWith('/super-admin)';      return 'Super Admin Dashboard';    } else if (route.startsWith('/compagnie-dashboard)';      return 'Admin Compagnie Dashboard';    } else if (route.startsWith('/agence-dashboard)';      return 'Admin Agence Dashboard';    } else if (route.startsWith('/agent-dashboard)';      return 'Agent Dashboard';    } else if (route.startsWith('/expert-dashboard)';      return 'Expert Dashboard';    } else if (route.startsWith('/conducteur-dashboard)';      return ';    } else if (route == login';      return ';    } else if (route == changePassword';      return 'Changer le mot de passe';    return 'Constat Tunisie';  static IconData getPageconst Icon(String route';    if (route.startsWith('/super-admin)';    } else if (route.startsWith('/compagnie-dashboard)';    } else if (route.startsWith('/agence-dashboard)';    } else if (route.startsWith('/agent-dashboard)';    } else if (route.startsWith('/expert-dashboard)';    } else if (route.startsWith(';