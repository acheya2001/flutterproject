import 'package:flutter/material.dart';import 'package:firebase_core/firebase_core.dart';import ';    ';    print(';  } catch (e';    print(';  Widget build(BuildContext context';      title: ';  Widget build(BuildContext context';        title: const Text(';            const SizedBox(height: 10')')';              'L\';              ';              child: const Text(';  Widget build(BuildContext context')')';        title: const Text(';          onPressed: () => _showTestDialog(context')')';          child: const Text(';      builder: (context')')';        title: const Text('🎉 Test Reussi !')')';            const Text(';            const SizedBox(height: 16')')';            const Text(';            const SizedBox(height: 8')')';            const Text('Email: test@example.com')')';            const Text(';            onPressed: () => Navigator.of(context).pop(')')';            child: const Text(';')')