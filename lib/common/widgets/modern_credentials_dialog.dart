import 'package:flutter/material.dart';import ';    ';  /// 💡 Carte d';    final isPassword = label.toLowerCase(').contains(';                      label.toLowerCase(').contains(';  Widget _buildCopyAllButton(';        label: const Text(';        content: ({_formatLabel(label')')')} copie';    buffer.writeln(';    for (final entry in widget.credentials.entries';      buffer.writeln('${_formatLabel(entry.key)}: ';    buffer.writeln(';    buffer.writeln(';    ScaffoldMessenger.of(context';        content: const Text(';    final lowerLabel = label.toLowerCase(')')';    if (lowerLabel.contains('email)';    if (lowerLabel.contains('password') || lowerLabel.contains('mot de passe)';    if (lowerLabel.contains('nom)';    if (lowerLabel.contains('role)';    if (lowerLabel.contains(';    switch (label.toLowerCase()';      case 'email': return 'Email';      case 'password': return 'Mot de passe';      case 'nom': return 'Nom complet';      case 'role': return 'Rôle';      case 'agence': return ';