import 'package:flutter/material.dart';import 'package:flutter/services.dart';                                content: Text('Code $sessionCode copié';                // Informations de l';                                'Conducteurs';                                '$conducteursRejoints/$nombreConducteurs';                    // Bouton d';                            isComplete ? 'Terminé' : 'Rejoindre';      return 'Aujourd\'hui ${date.hour.toString().padLeft(2, '0')}:${date.minute.toString().padLeft(2, '0')}';      return 'Hier ${date.hour.toString().padLeft(2, '0')}:${date.minute.toString().padLeft(2, '0')}';      return '${date.day}/${date.month}/${date.year}';