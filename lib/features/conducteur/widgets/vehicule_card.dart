import 'package:flutter/material.dart';import 'package:intl/intl.dart';import '../../vehicule/models/vehicule_model.dart';import '../../vehicule/screens/vehicule_detail_screen.dart';    final dateFormat = DateFormat('dd/MM/yyyy';    // Couleurs dynamiques selon l';    // Couleurs selon l';    String statusText = isExpired ? 'Expiré' : 'Valide';                      'Marque';                      vehicule.marque.isNotEmpty ? vehicule.marque : 'Non spécifié';                      'Assureur';                      vehicule.compagnieAssurance.isNotEmpty ? vehicule.compagnieAssurance : 'Non spécifié';                      'Modèle';                      vehicule.modele.isNotEmpty ? vehicule.modele : 'Non spécifié';                      'Validité';                      vehicule.dateFinValidite != null ? dateFormat.format(vehicule.dateFinValidite!) : 'Non spécifié';          // Boutons d';                        // Navigation vers l';                              'Voir les détails';                                'Déclarer un sinistre';}