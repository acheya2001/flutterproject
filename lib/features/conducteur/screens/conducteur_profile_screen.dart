import 'package:flutter/material.dart';import 'package:provider/provider.dart';import '../../../core/widgets/custom_app_bar.dart';import '../../../features/auth/providers/auth_provider.dart';        title: 'Mon profil';                          ? '${user.prenom[0]}${user.nom[0]}';                          : 'U';                    '${user?.prenom ?? ''} ${user?.nom ?? ''}';                    'Conducteur';              'Informations personnelles';            _buildInfoItem(Icons.email, 'Email', user?.email ?? 'Non renseigné';            _buildInfoItem(Icons.phone, 'Téléphone', user?.telephone ?? 'Non renseigné';            _buildInfoItem(Icons.location_on, 'Adresse', user?.adresse ?? 'Non renseignée';            // Boutons d';                // Naviguer vers l';                  const SnackBar(content: Text('Modification du profil à venir';              child: const Text('Modifier mon profil';                // Naviguer vers l';                  const SnackBar(content: Text('Modification du mot de passe à venir';              child: const Text('Changer mon mot de passe';}