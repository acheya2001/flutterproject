import 'package:flutter/material.dart';                    ';                    ({compagnieData['code'] ?? 'N/A';                ';                  _getStatusconst Text(compagnieData[';          const SizedBox(height: 20';            'Type d\'assurance';            compagnieData['type'] ?? ';          const SizedBox(height: 12';            'Email';            compagnieData['email'] ?? ';          const SizedBox(height: 12';            'Telephone';            compagnieData['telephone'] ?? ';          const SizedBox(height: 12';            'Adresse';            compagnieData['adresse'] ?? ';          ';          if (compagnieData[';                  const SizedBox(width: 6';                  ({_formatDate(compagnieData['created_at]')}';        const SizedBox(width: 8';        (label: ';    final statusStr = status?.toString().toLowerCase() ?? ';    switch (statusStr';      case ';        return const Color(0xFF10B981';      case ';        return const Color(0xFF6B7280';      case ';    final statusStr = status?.toString().toLowerCase(') ?? ';    switch (statusStr';      case 'actif';        return 'Actif';      case 'inactif';        return 'Inactif';      case 'suspendu';        return 'Suspendu';        return ';      if (timestamp == null') return ';      } else if (timestamp.runtimeType.toString(').contains(';        date = timestamp.toDate(';        return 'Format invalide';      return '{date.day.toString(').padLeft(2, '0')}/'{date.month.toString().padLeft(2, '0')}/';    } catch (e';      return ';