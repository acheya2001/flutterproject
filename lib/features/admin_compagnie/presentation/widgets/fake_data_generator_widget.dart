import 'package:flutter/foundation.dart';import 'package:flutter/material.dart';import ';  State<FakeDataGeneratorWidget> createState() => _FakeDataGeneratorWidgetState(';  String _currentProgress = ';    } catch (e';      debugPrint(';    setState((';      _currentProgress = ';      if (mounted';          title: ';      if (mounted';          title: '❌ Erreur de generation';          content: 'Une erreur est survenue lors de la generation des donnees :\n\n';        setState((';          _currentProgress = ';    if (_isClearing';      title: '🗑️ Supprimer les donnees de test';      content: 'Êtes-vous sûr de vouloir supprimer toutes les donnees factices de cette compagnie ?\n\n';               ';      if (mounted';          title: '✅ Suppression reussie';          content: 'Donnees supprimees :\n';                   '• '{results['agences']} agences\n';                   '• '{results['employes';      if (mounted';          title: '❌ Erreur de suppression';          content: 'Une erreur est survenue lors de la suppression :\n\n';            onPressed: () => Navigator.pop(context';            child: const Text(';            onPressed: () => Navigator.pop(context, false')')';            child: const Text(';            ')')';            child: const Text(';          if (_existingData != null')')';            ({_existingData!['employes';            const SizedBox(height: 12';          // Boutons d';                  label: const Text(_isGenerating ? 'Generation...' : ';              // Bouton supprimer (si des donnees existent';                  (_existingData!['agences']! > 0 || _existingData![';                          ';                    label: const Text(_isClearing ? 'Suppression...' : ';          const SizedBox(height: 8';