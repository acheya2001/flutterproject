import 'dart:io';import 'dart:math';import 'package:flutter/foundation.dart';import '../models/accident_analysis_model.dart';/// 🤖 Service d';  /// 📸 Analyse OFFLINE des images d';      debugPrint('[OfflineAI] 🚀 Début de l\'analyse IA OFFLINE';      // Simulation d';          .map((entry) => 'offline://image_${entry.key}_${DateTime.now().millisecondsSinceEpoch}';      // 5. Création de l';      debugPrint('[OfflineAI] ✅ Analyse IA OFFLINE terminée avec succès';      debugPrint('[OfflineAI] ❌ Erreur lors de l\'analyse IA: $e';    debugPrint('[OfflineAI] 🔍 Analyse des images en mode offline';    final accidentTypes = ['intersection', 'rattrapage', 'frontal', 'stationnement';        id: 'vehicle_${String.fromCharCode(65 + i)}';    // Analyse d';        'source': 'Analyse IA offline';        'method': 'pattern_recognition_offline';        'accident_type';        'processing_time': '${2 + Random().nextInt(3)}s';    final description = textDescription ?? voiceDescription ?? '';    final prompt = ''';${imageAnalysis.vehicles.map((v) => '• ${v.type} ${v.color} - Position: ${v.position}').join('\n';💥 ANALYSE DE L';${imageAnalysis.damages.map((d) => '• ${d.location} (${d.severity})').join('\n';"${description.originalText.isNotEmpty ? description.originalText : 'Collision entre véhicules';2. ${description.timeline.length > 1 ? description.timeline[1] : 'Moment critique';''';    if (accidentType == 'intersection';      return index == 0 ? 'Berline' : 'Citadine';    } else if (accidentType == 'rattrapage';      return index == 0 ? 'SUV' : 'Berline';    final types = ['Berline', 'Citadine', 'SUV', 'Break';    final colors = ['Blanc', 'Noir', 'Gris', 'Rouge', 'Bleu';    if (accidentType == 'intersection';      final positions = ['Nord', 'Sud', 'Est', 'Ouest';    } else if (accidentType == 'rattrapage';      return index == 0 ? 'Devant' : 'Derrière';    return 'Position ${index + 1}';    if (accidentType == 'intersection';        DamageInfo(location: 'Côté droit', severity: 'Grave', description: 'Impact latéral';        DamageInfo(location: 'Côté gauche', severity: 'Modéré', description: 'Impact secondaire';    } else if (accidentType == 'rattrapage';        DamageInfo(location: 'Arrière', severity: 'Grave', description: 'Impact arrière';        DamageInfo(location: 'Avant', severity: 'Modéré', description: 'Déformation avant';      DamageInfo(location: 'Avant', severity: 'Grave', description: 'Impact frontal';      case 'intersection';        return ImpactAnalysis(direction: 'Latéral', angle: '90°', speed: 'Modérée';      case 'rattrapage';        return ImpactAnalysis(direction: 'Arrière', angle: '0°', speed: 'Faible';        return ImpactAnalysis(direction: 'Frontal', angle: '180°', speed: 'Élevée';    final words = text.toLowerCase().split(' ';    final importantWords = ['collision', 'choc', 'accident', 'freinage', 'vitesse', 'intersection';    return keywords.isEmpty ? ['accident', 'collision';    if (text.contains('intersection') facts.add('Accident à une intersection';    if (text.contains('vitesse') facts.add('Vitesse impliquée';    if (text.contains('pluie') facts.add('Conditions météo défavorables';    if (text.contains('priorité') facts.add('Non-respect de priorité';    return facts.isEmpty ? ['Collision entre véhicules';    timeline.add('1. Approche des véhicules';    if (text.contains('freinage') timeline.add('2. Tentative de freinage';    timeline.add('3. Impact';    timeline.add('4. Arrêt des véhicules';  /// 📋 Vérification si l';  /// 📖 Récupération de l';