import 'package:flutter/material.dart';import '../../../../core/theme/modern_theme.dart';import '../../../../core/services/role_navigation_service.dart';import '../../../../core/services/offline_admin_service.dart';/// Gere l';    ';  /// 📋 Construire l';              labelText: ';              if (value == null || value.isEmpty';                return 'Veuillez saisir votre email';              if (!RegExp(r^[\w-\.]+@([\w-]+\.')+[\w-]{2,4}').hasMatch(value)';                return ';          const SizedBox(height: 16';              labelText: ';              if (value == null || value.isEmpty';                return ';      final email = _emailController.text.trim(';      debugPrint('[LOGIN] 🔐 Tentative de connexion: 'email';      // 1. Essayer d';        debugPrint(';      } catch (e';        debugPrint(';        ';        if (offlineResult['success]';          debugPrint('[LOGIN] ✅ Connexion hors ligne reussie';          // Afficher un message d';                content: const Text(';            ')')';          // TODO: Rediriger vers l';          // Pour l';          _showOfflineAccountDialog(offlineResult['admin]';          throw Exception(';      if (mounted';        debugPrint(';    } catch (e';      debugPrint(';      if (mounted';        String errorMessage = ';        if (e.toString(').contains('user-not-found)';          errorMessage = ';        } else if (e.toString(').contains('wrong-password)';          errorMessage = ';        } else if (e.toString(').contains('invalid-email)';          errorMessage = 'Format d\';        } else if (e.toString(').contains('user-disabled)';          errorMessage = ';        } else if (e.toString(').contains('too-many-requests)';          errorMessage = ';            const SizedBox(width: 8';            const Text(';        ')')';            ({adminData['nom';            const SizedBox(height: 8';            ({adminData['role']}';            ({adminData['compagnieNom';              ';                ';              Navigator.of(context).pop(';              // TODO: Rediriger vers l';            child: const Text(';')')