import 'package:flutter/material.dart';import 'insurance_colors.dart';/// 🛠️ Utilitaires pour l';    return '${amount.toStringAsFixed(2)} TND';    return '${date.day.toString().padLeft(2, '0')}/${date.month.toString().padLeft(2, '0')}/${date.year}';    if (!isActive) return 'Expiré';    if (isExpiring) return 'Expire bientôt';    return 'Actif';