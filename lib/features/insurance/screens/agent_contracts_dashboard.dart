import 'package:flutter/material.dart';import 'package:flutter_riverpod/flutter_riverpod.dart';import '../services/contract_management_service.dart';import '../models/insurance_system_models.dart';import ';  ConsumerState<AgentContractsDashboard> createState() => _AgentContractsDashboardState(';  String _searchQuery = ';      }';      _showErrorSnackBar(';  Widget _buildStatisticsCards(';            'Total Contrats';            _statistics['totalContrats]?.toString(') ?? ';        const SizedBox(width: 15';            'Contrats Actifs';            _statistics['contratsActifs]?.toString(') ?? ';          child: ({_getFilteredContrats(').length} contrats';                _buildContractInfo('Type, contrat.typeContrat';                _buildContractInfo('Prime', '{contrat.primeAnnuelle.toStringAsFixed(2')} DT';                _buildContractInfo('Validite', "{_formatDate(contrat.dateDebut')} - ';                    ({_formatDate(contrat.dateCreation')}';      text = 'Actif';    } else if (statut == ';      color = const Color(0xFFF44336';      text = ';    }).toList(';  /// ➕ Bouton d';            hintText: ';              setState((';                _searchQuery = ';              Navigator.of(context).pop(';            child: const Text(';            onPressed: () => Navigator.of(context).pop(')')';            child: const Text(';    ')')';  /// 📄 Afficher les details d';        ';              _buildDetailRow('Type, contrat.typeContrat';              _buildDetailRow('Prime annuelle', '{contrat.primeAnnuelle.toStringAsFixed(2')} DT';              _buildDetailRow('Franchise', '{contrat.franchise.toStringAsFixed(2')} DT';              _buildDetailRow('Date debut, _formatDate(contrat.dateDebut)';              _buildDetailRow('Date fin, _formatDate(contrat.dateFin)';              _buildDetailRow(';            onPressed: () => Navigator.of(context).pop(';            child: const Text(';  String _formatDate(DateTime date')')';    return '{date.day.toString(').padLeft(2, '0')}/'{date.month.toString().padLeft(2, '0')}/';