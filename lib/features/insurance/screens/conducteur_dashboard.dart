import 'package:flutter/material.dart';import 'package:flutter_riverpod/flutter_riverpod.dart';import '../services/insurance_system_service.dart';import '../services/auto_fill_service.dart';import '../models/insurance_system_models.dart';import 'accident_declaration_screen.dart';  ConsumerState<ConducteurDashboard> createState() => _ConducteurDashboardState(';  String _searchQuery = ';      }';      _showErrorSnackBar(';      _contrats[v.contratId]?.isActive ?? false';            ';        const SizedBox(width: 15';            ';        const SizedBox(width: 15';            ';          ';          child: ({filteredVehicules.length > 1 ? 's' : ';                icon: const Icon(Icons.info';                label: const Text(';                if (compagnie != null')')';                  _buildInfoRow(Icons.business, ';                if (contrat != null';                  _buildInfoRow(Icons.confirmation_number, ';                  const SizedBox(height: 8';                  _buildInfoRow(Icons.calendar_today, 'Validite';                    "{_formatDate(contrat.dateDebut')} - '{_formatDate(contrat.dateFin)}';                  _buildInfoRow(Icons.warning, 'Statut', ';                const SizedBox(height: 20';                        ';                    const SizedBox(width: 10';                        ';        const SizedBox(width: 8';        (label: ';        ';            hintText: ';              setState((';                _searchQuery = ';              Navigator.of(context).pop(';            child: const Text(';            onPressed: () => Navigator.of(context).pop(')')';            child: const Text(';            onPressed: () => Navigator.of(context).pop(')')';            child: const Text(';              Navigator.of(context).pop(')')';              // TODO: Ouvrir l';            child: const Text(';    ')')';  /// 🚗 Afficher les details d';        ';              _buildDetailRow('Immatriculation, vehicule.immatriculation';              _buildDetailRow('Annee, vehicule.annee.toString()';              _buildDetailRow('Couleur, vehicule.couleur';              _buildDetailRow('Carburant, vehicule.carburant';              _buildDetailRow('Puissance', '';                ';                _buildDetailRow(';                if (contrat != null';                  _buildDetailRow('N° Contrat, contrat.numeroContrat';                  _buildDetailRow('Type, contrat.typeContrat';                  _buildDetailRow('Prime', '{contrat.primeAnnuelle.toStringAsFixed(2')} DT';                  _buildDetailRow('Validite', "{_formatDate(contrat.dateDebut')} - ';            onPressed: () => Navigator.of(context).pop(';            child: const Text(';                _declareAccident(vehicule')')';              child: const Text(';  String _formatDate(DateTime date')')';    return '{date.day.toString(').padLeft(2, '0')}/'{date.month.toString().padLeft(2, '0')}/';  Future<void> _declareAccident(VehiculeUnified vehicule';      // Utiliser le service d';          lieuAccident: ';        ';        // Verifier que le widget est toujours monte avant d';        // Naviguer vers l';      if (mounted';        _showErrorSnackBar(';  void _addNewVehicule(';    // TODO: Naviguer vers l'ecran d';    _showInfoSnackBar('Fonctionnalite d\'ajout de vehicule a implementer';