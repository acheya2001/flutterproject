import 'package:flutter/material.dart';import '../../models/professional_request_model_final.dart';import '../../../../core/theme/modern_theme.dart';            _buildInfoRow(Icons.email, 'Email';            _buildInfoRow(Icons.phone, 'Téléphone';            _buildInfoRow(Icons.credit_card, 'CIN';            if (request.roleDemande == 'agent_agence';              _buildInfoRow(Icons.business, 'Agence', request.nomAgence ?? 'N/A';              _buildInfoRow(Icons.domain, 'Compagnie', request.compagnie ?? 'N/A';            if (request.roleDemande == 'expert_auto';              _buildInfoRow(Icons.verified, 'N° Agrément', request.numAgrement ?? 'N/A';              _buildInfoRow(Icons.location_on, 'Zone', request.zoneIntervention ?? 'N/A';            if (request.roleDemande == 'admin_compagnie';              _buildInfoRow(Icons.corporate_fare, 'Compagnie', request.nomCompagnie ?? 'N/A';              _buildInfoRow(Icons.work, 'Fonction', request.fonction ?? 'N/A';                  'Soumis le ${_formatDate(request.envoyeLe)}';            // Boutons d';            if (request.status == 'en_attente';                              'Voir les détails';      case 'en_attente';        label = 'En attente';      case 'acceptee';        label = 'Approuvée';      case 'rejetee';        label = 'Rejetée';        label = 'Inconnu';  /// 📝 Ligne d';          '$label: ';  /// 🎯 Boutons d';            label: const Text('Approuver';            label: const Text('Rejeter';    if (date == null) return 'Date inconnue';      return 'Aujourd\'hui à ${date.hour.toString().padLeft(2, '0')}:${date.minute.toString().padLeft(2, '0')}';      return 'Hier à ${date.hour.toString().padLeft(2, '0')}:${date.minute.toString().padLeft(2, '0')}';      return 'Il y a ${difference.inDays} jours';      return '${date.day.toString().padLeft(2, '0')}/${date.month.toString().padLeft(2, '0')}/${date.year}';