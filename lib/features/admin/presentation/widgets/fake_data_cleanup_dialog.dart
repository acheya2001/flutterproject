import 'package:flutter/material.dart';import ';      builder: (context';        title: const Text('⚠️ Confirmation')')';          'Êtes-vous sûr de vouloir supprimer TOUTES les donnees fake ?\n\n';          'Cette action est irreversible et supprimera tous les documents ';          ';            onPressed: () => Navigator.of(context).pop(false';            child: const Text(';            ')')';            child: const Text(';                            const SizedBox(height: 16')')';                            const Text(';            const SizedBox(height: 24')')';            // Boutons d';                    label: const Text(';                          ')')';                    label: const Text(_isLoading ? 'Nettoyage...' : ';  Widget _buildStatusSection(';    final hasFakeData = status['hasFakeData';    final totalFake = status['totalFake';    final details = status[';          const SizedBox(height: 8';          (totalFake documents fake trouves dans la base de donnees';                : ';                        ';                        child: ({entry.value}';    final success = result['success';    final totalDeleted = result['totalDeleted';    final details = result[';          const SizedBox(height: 8';          (totalDeleted documents fake supprimes avec succes';                : result['message'] ?? ';                        ';