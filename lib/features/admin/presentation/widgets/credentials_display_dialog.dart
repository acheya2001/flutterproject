import 'package:flutter/material.dart';import 'package:flutter/services.dart';  Widget build(BuildContext context';    final email = widget.credentials['email';    final password = widget.credentials[';            const SizedBox(height: 24';            // Message d';              label: ';              onCopy: (') => _copyToClipboard(email, ';            const SizedBox(height: 16';              label: ';              onCopy: (') => _copyToClipboard(password, ';                  const SizedBox(height: 8';                  _buildInfoItem('• Ce compte est institutionnel (pas personnel)';                  _buildInfoItem('• L\'Admin Compagnie peut creer des agences et leurs admins';                  _buildInfoItem('• Acces limite aux donnees de sa compagnie uniquement';                  _buildInfoItem('• Aucun email automatique n\';            const SizedBox(height: 24';            // Boutons d';                    label: const Text(';                    icon: const Icon(Icons.info')')';                    label: const Text(';    setState((')')';      if (type == 'email';      } else if (type == ';    }';    // Reinitialiser l';          if (type == 'email';          } else if (type == ';    ScaffoldMessenger.of(context';        content: ({type == 'email' ? 'Email' : 'Mot de passe';  void _copyAllCredentials(';    final email = widget.credentials['email';    final password = widget.credentials['password';    final allCredentials = ';Compagnie: ';- Transmettez ces identifiants a la compagnie d';- L';';    ScaffoldMessenger.of(context';        content: const Text(';')')