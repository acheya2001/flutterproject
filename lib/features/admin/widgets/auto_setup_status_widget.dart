import 'package:flutter/material.dart';import '../../../core/services/auto_setup_service.dart';/// 📊 Widget qui affiche le statut de l';  const Text(';        setState((')')';          _setupStatus = {'error: e.toString('), 'status': ';              const SizedBox(width: 12';              const Text(';                const SizedBox(width: 8')')';                ({isOK ? 'Configure' : 'En cours';                if (!isOK';                    child: const Text(';            const SizedBox(height: 8')')';            ({_setupStatus!['users_collection_exists'] ? '✅' : '❌'}';            ({adminCount >= 4 ? '✅' : '❌'}';            if (status == 'ERROR';              ({_setupStatus!['error';        ScaffoldMessenger.of(context';            content: const Text(';        ScaffoldMessenger.of(context')')';