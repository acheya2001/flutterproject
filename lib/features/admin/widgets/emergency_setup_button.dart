import 'package:flutter/material.dart';import '../../../core/services/firestore_connection_manager.dart';/// 🚨 Bouton d';  const Text(';              const SizedBox(height: 8')')';                'Si l\'auto-setup ne fonctionne pas a cause de problemes de connexion, ';                ';                    icon: const Icon(Icons.info';                    label: const Text(';                    icon: const Icon(Icons.info')')';                    label: const Text(';      ScaffoldMessenger.of(context')')';          content: const Text(';        ScaffoldMessenger.of(context')')';              ? '✅ Connexion Firestore reparee !';              : ';        ScaffoldMessenger.of(context';            content: (e';          content: const Text(';      if (mounted')')';        final success = result['success';        final createdCount = (result['created_admins] as List';        final failedCount = (result[';        ScaffoldMessenger.of(context';            content: (failedCount admins non crees';      builder: (context';        title: const Text('📊 Statut Connexion')')';            ({status['internet'] ? '✅' : '❌'}';            ({status['firestore'] ? '✅' : '❌'}';            ({status['optimized'] ? '✅' : '❌'}';            ({status['overall_status']}';            if (status['error] != null';              ({status['error';            onPressed: () => Navigator.pop(context';            child: const Text(';      builder: (context')')';        title: const Text('🎉 Succes !')')';            ({(result['created_admins] as List').length} admins crees:';            ...(result[';              (email') => (email';            const Text('🎯 Vous pouvez maintenant:')')';            const Text('• Utiliser le Super Admin Dashboard')')';            const Text('• Creer d\')')'autres utilisateurs';            const Text('• Gerer les compagnies d\')')';            onPressed: () => Navigator.pop(context';            child: const Text(';    ')')';  /// ❌ Afficher le dialog d';        title: const Text('❌ Probleme')')';            if ((result['failed_admins] as List).isNotEmpty';              ({(result['failed_admins] as List').length}';              ...(result[';                (email') => (email';            const Text('💡 Solutions:')')';            const Text('• Verifier la connexion Internet')')';            const Text('• Reessayer dans quelques minutes')')';            const Text(';            onPressed: () => Navigator.pop(context')')';            child: const Text(';')')