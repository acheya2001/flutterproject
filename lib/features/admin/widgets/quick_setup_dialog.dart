import 'package:flutter/material.dart';import '../../../core/services/instant_users_collection_creator.dart';import '../../../core/services/super_admin_performance_optimizer.dart';  const Text(';  State<QuickSetupDialog> createState() => _QuickSetupDialogState(')')';  String _currentStep = ';          const SizedBox(width: 8';          const Text(';                  const SizedBox(height: 4')')';                  const Text('• Collection "users" n\')')'existe pas';                  const Text('• Interface Super Admin lente')')';                  const Text(';            onPressed: () => Navigator.pop(context, false')')';            child: const Text(';            ')')';            child: const Text(';          const SizedBox(width: 8')')';          const Text(';      setState((')')') => _currentStep = ';        setState((';          _completedSteps.add('Firestore optimise';          _currentStep = ';      } catch (e';        _errors.add(';      setState((') => _currentStep = ';        final result = await InstantUsersCollectionCreator.createUsersCollectionNow(';        if (result['success] == true';          final count = (result[';          setState((';            _completedSteps.add('Collection users creee (count admins')';            _currentStep = ';          }';          _errors.add(';      } catch (e';        _errors.add(';      setState((') => _currentStep = ';        setState((';          _completedSteps.add('Donnees pre-chargees';          _currentStep = ';      } catch (e';        _errors.add(';      setState((') => _currentStep = ';      setState((';        _completedSteps.add('Configuration terminee';        _currentStep = ';      setState((';        _errors.add('Erreur generale:  + e.toString()';        _currentStep = ';